import 'dart:ui';

class AppColors {
  /// Bold Charcoal - Main primary color (powerful, timeless)
  static const Color primary = Color(0xFF2B2B2B);

  /// Warm Brick - Secondary accent (attention-grabbing but classy)
  static const Color secondary = Color(0xFFB84C32);

  /// Burnt Copper - Button hovers, highlights
  static const Color primaryLight = Color(0xFFD76C4A);

  /// Deep Umber - App bars, focus borders
  static const Color primaryDark = Color(0xFF1A1A1A);

  /// Soft Cream - Background for screens
  static const Color background = Color(0xFFFBF9F6);

  /// Clean White - Cards and surfaces
  static const Color surface = Color(0xFFFFFFFF);

  /// Strong Slate - Title/headlines
  static const Color titleText = Color(0xFF1C1C1C);

  /// Graphite Gray - Body text
  static const Color textPrimary = Color(0xFF3D3D3D);

  /// Taupe Gray - Hint/secondary text
  static const Color textSecondary = Color(0xFF7C7C7C);

  /// Muted Ash - Form labels
  static const Color labelText = Color(0xFF5F5F5F);

  /// Brick Red - Error color (not bright red, but clearly visible)
  static const Color error = Color(0xFFA02424);
}
